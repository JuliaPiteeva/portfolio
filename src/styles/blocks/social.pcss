.socials {
  display: flex;
  align-items: center;
}

.socials__icon {
  fill: currentColor;
  width: 100%;
}
.socials__item {
  margin-right: 40px;
  &:last-child {
    margin-right: 0;
  }
}
.social__link {
  width: 34px;
  height: 27px;
  display: block;
}
