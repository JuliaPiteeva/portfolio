.hero-container {
  display: flex;
  flex-direction: column;
  align-content: space-between;
  align-items: center;
}
.hero {
  height: 100vh;
  position: relative;
  min-height: 650px;
  margin-bottom: 100px;

  @include phones {
    margin-bottom: 0;
  }
}
.hero__content {
}
.hero__bottom-btn {
  @include tablets {
    display: none;
  }
}
