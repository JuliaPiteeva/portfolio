.welcome-section {
  position: relative;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  @include tablets {
  }
}
.welcome-section__bg-img {
  position: absolute;
  z-index: -1;
  top: 0;
  right: 0;
  width: 100%;
  height: 100%;
  display: none;
   {
  }
}
