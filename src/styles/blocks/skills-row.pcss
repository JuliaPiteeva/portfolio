.skills-row__list {
  display: flex;
  /* justify-content: space-between;
  align-items: center; */
}
.skills-row__title {
  font-size: 18px;
  color: $gray-color;
  font-family: "Open Sans";
  font-weight: 700;
  margin-bottom: 30px;
}
.skills-row__item {
  position: relative;
  margin-right: 40px;
  &:last-child {
    margin-right: 0;
  }
}
